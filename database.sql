-- Data base imobiliaria
CREATE DATABASE imobiliaria;
USE imobiliaria;

table => user{
	
	CREATE TABLE usuario(

		id int aut inc primary key;
		nome varchar(20);
		bi varchar(20);
		email vatchar(50);
		senha_crip(varchar(100);
		senha varchar(20);
		nivel varchar(10);
	)
		SELECT * FROM imobiliario;
}
